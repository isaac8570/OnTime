<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.OnTime.ontime">

    <!-- 포그라운드 서비스 권한 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
      Android 14 (API 34) 이상에서 위치 정보 포그라운드 서비스를 위해 추가해야 하는 권한
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:roundIcon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".ui.screen.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false" />

        <!-- NotificationHistoryActivity removed as per user request -->

        <activity
            android:name=".DataCollectionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.TestPageActivity"
            android:exported="false" />

        <activity
            android:name=".ui.NotificationListPageActivity"
            android:exported="false" />

        <activity
            android:name=".ui.CustomLocationSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AddCustomLocationMapActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AlarmTestActivity"
            android:exported="false" />

        <!-- 알림 수신자 -->
        <receiver
            android:name=".service.NotificationReceiver"
            android:exported="false" />

        <receiver
            android:name=".service.FollowUpNotificationReceiver"
            android:exported="false" />

        <!-- LocationService 선언 수정 -->
        <service
            android:name=".service.LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
